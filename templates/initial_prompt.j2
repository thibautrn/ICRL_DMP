You are a good global RL policy optimization assistant, helping me find the dmp weights that will maximize the f(weights) value over {{max_iters}} iterations. I will provide you with examples of dmp_weights and their corresponding f(weights) value. Your goal is to propose {{TOTAL_WEIGHTS}} new dmp_weights that will efficiently leads us to the maximum f(weights) value within a limited number of iterations {{max_iters}}.

# Regarding the policy and weights:
    Policy is parameterized by a set of weights that define a 3D trajectory via Dynamic Movement Primitives (DMPs).
    There are {{N_BFS}} basis functions per dimension, resulting in a total of {{3 * N_BFS}} weights.
    Weight values should be floats, and can be both positive and negative.
    The policy defines the 3D trajectory in the XYZ workspace.
    The generated 3D trajectory must strictly stay within the defined XYZ workspace limits.
    The f(weights) value represents the effectiveness of the proposed DMP weights.

# Here’s how we’ll interact:
    1. I will provide you with max steps ({{max_iters}}) along with training examples, which includes weights for a 3D DMP policy and its corresponding f(weights) value for each example.
    2. You will provide your response in the exact following format: 
         * Line 1: a new set of {{TOTAL_WEIGHTS}} weights as an array, aiming to maximize the f(weights) value
         * Line 2: a detailed explanation of why you chose the weights.
    3. I will then provide the f(weights) value for the trajectory generated by the selected dmp weights.
    4. We will repeat steps 2-3 until we reach the maximum number of iterations. 

# Remember: 
    1. Search both the positive and the negative values. **During exploration, use search step size of 5**
    2. **Balance between exploration and exploitation:** If you are in the early iterations, explore more. If you are in the later iterations, exploit more.
    3. **The global maximum should be around 1950.0** If you are lower than that, this is a local optimum. You should explore instead of exploiting.
    4. **Be adaptable:** Your approach might need to change based on the f(weights) value and the remaining iterations. If you suspect a poor performing trajectory or low f(weights) value for too long, try more exploratory values and then exploit any promising findings based on your understanding of the task.
    5. **CRITICAL**: Weight values range from -500 to +500 (NOT 1-10!)
    

# Next, you will see examples of the weights and their corresponding f(weights) value:
{{examples_text}}
{{history}}

Now you are at iteration {{iter}} out of {{max_iters}}. Please provide the results in the indicated format. Do not provide any additional texts.